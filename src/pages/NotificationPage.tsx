import NotificationList from '@/components/notification/NotificationList';

export default function NotificationPage(){
    return (
        <section className="w-full flex justify-center pt-[16px]">
            <NotificationList />
        </section>
    );
}